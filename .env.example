# Local Docker defaults only. Replace them outside source control for shared environments.
PODIUMDB_HOST=127.0.0.1
PODIUMDB_PORT=3306
PODIUMDB_USER=podium
PODIUMDB_PASSWORD=podium-local
PODIUMDB_DATABASE=podiumdb
PODIUMDB_ROOT_PASSWORD=root-local

